document.getElementById("charge").addEventListener("click", withdrawal());
document.getElementById("payment").addEventListener("click", deposit());

let withdrawals = [];

// withdrawal function
function withdrawal() {
    withdrawals.forEach((charge) => {
        const { company, amount } = charge;
    })
}